export const setConfigurationSection = section => ({ type: 'setConfigurationSection', payload: section });

export const resetConfigurationSection = () => ({ type: 'resetConfigurationSection', payload: null });
