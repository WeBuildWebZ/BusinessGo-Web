import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  spinner: {},
  background: {
    position: 'absolute',
    left: 0,
    top: 0,
    width: '100%',
    height: '100%',
    backgroundColor: '#55F'
  }
});
