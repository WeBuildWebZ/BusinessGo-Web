import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  inputLabel: {
    textAlign: 'center',
    margin: 10
  },
  formControl: {},
  select: {
    margin: 10,
    padding: 5,
    textAlign: 'center'
  }
});
