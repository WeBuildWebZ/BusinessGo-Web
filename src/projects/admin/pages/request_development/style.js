import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  paragraph: {
    maxWidth: '70%'
  },
  flowItem: {
    fontSize: 11,
    textAlign: 'left',
    maxWidth: 500
  }
});
