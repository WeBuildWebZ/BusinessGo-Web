import React from 'react';
import ReactDOM from 'react-dom';

import Chatbot from '../../../components/Chatbot';

ReactDOM.render(
  <React.StrictMode>Hola soy una app de React!</React.StrictMode>,
  document.getElementById('root')
);
