export const getLanguage = langCode =>
  ({
    es: {
      title: 'Editor de Formularios'
    },
    en: {
      title: 'Form Editor'
    }
  }[langCode]);

const Component = () => <div />;

export default Component;
