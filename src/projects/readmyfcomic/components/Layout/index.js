import Title from './Title';

const Layout = () => (
  <div className="layout">
    <Title />
    <style jsx>
      {`
        .layout {
        }
      `}
    </style>
  </div>
);

export default Layout;
